apiVersion: ai.platform.io/v1
kind: ModelConfig

metadata:
  name: openai

spec:
  provider: openai

  models:
    primary: gpt-5.5
    fallback: gpt-5-mini

  limits:
    maxTokens: 128000

  retries:
    maxAttempts: 3