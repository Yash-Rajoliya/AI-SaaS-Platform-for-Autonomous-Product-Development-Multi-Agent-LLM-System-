apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability-app
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/company/ai-saas-platform
    targetRevision: main
    path: infrastructure/kubernetes/monitoring

  destination:
    server: https://kubernetes.default.svc
    namespace: observability

  syncPolicy:
    automated:
      prune: true
      selfHeal: true