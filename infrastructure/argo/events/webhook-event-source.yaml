apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: webhook-event-source
  namespace: argo-events
spec:
  service:
    ports:
      - port: 12000
         

  webhook:
    deployment:
      endpoint: /deploy
      method: POST
      port: "12000"