apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-config
  namespace: platform

data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  REDIS_HOST: "redis"
  POSTGRES_HOST: "postgres"
  KAFKA_BROKER: "kafka:9092"