apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: internal-services-policy
  namespace: platform

spec:
  podSelector: {}

  policyTypes:
    - Ingress

  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              access: internal