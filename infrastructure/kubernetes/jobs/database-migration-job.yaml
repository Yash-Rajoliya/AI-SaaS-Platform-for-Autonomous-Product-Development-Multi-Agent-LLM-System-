apiVersion: batch/v1
kind: Job
metadata:
  name: database-migration
  namespace: platform
spec:
  backoffLimit: 3

  template:
    spec:
      restartPolicy: Never

      containers:
        - name: migrate
          image: platform/platform-core:latest

          command:
            - npm
            - run
            - migrate