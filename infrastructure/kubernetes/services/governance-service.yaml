apiVersion: v1
kind: Service
metadata:
  name: governance-service
  namespace: security
spec:
  selector:
    app: governance-service
  ports:
    - port: 80
      targetPort: 8085
  type: ClusterIP