apiVersion: v1
kind: Service
metadata:
  name: deployment-service
  namespace: ai-runtime
spec:
  selector:
    app: deployment-service
  ports:
    - port: 80
      targetPort: 8086
  type: ClusterIP