apiVersion: v1
kind: Service
metadata:
  name: monitoring-service
  namespace: observability
spec:
  selector:
    app: monitoring-service
  ports:
    - port: 80
      targetPort: 8084
  type: ClusterIP