apiVersion: v1
kind: Service
metadata:
  name: execution-service
  namespace: platform
spec:
  selector:
    app: execution-service
  ports:
    - port: 80
      targetPort: 8083
  type: ClusterIP