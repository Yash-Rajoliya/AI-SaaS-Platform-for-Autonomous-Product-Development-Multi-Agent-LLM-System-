apiVersion: v1
kind: Service
metadata:
  name: agent-service
  namespace: ai-runtime
spec:
  selector:
    app: agent-service
  ports:
    - port: 80
      targetPort: 8082
  type: ClusterIP