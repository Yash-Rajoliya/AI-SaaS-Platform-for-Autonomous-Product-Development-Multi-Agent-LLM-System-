apiVersion: 1

datasources:
  - name: Tempo
    uid: tempo
    type: tempo
    access: proxy
    url: http://tempo.platform.svc.cluster.local:3200
    editable: true

    jsonData:
      nodeGraph:
        enabled: true

      serviceMap:
        datasourceUid: prometheus

      tracesToLogs:
        datasourceUid: loki

      tracesToMetrics:
        datasourceUid: prometheus