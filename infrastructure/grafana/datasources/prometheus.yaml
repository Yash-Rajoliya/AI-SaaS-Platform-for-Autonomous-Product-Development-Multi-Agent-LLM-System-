apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus.platform.svc.cluster.local:9090
    isDefault: true
    editable: true